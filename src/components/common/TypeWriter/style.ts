import styled from 'styled-components';

export const TypeWrap = styled.div`
  font: var(--font-paragraph-xl) Raleway;
  background: var(--purpleBlue);
  background-clip: text;
  -webkit-text-fill-color: transparent;
  padding: 1rem 0rem;
`;
